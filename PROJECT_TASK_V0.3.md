# StudyCopilot V0.3 — 框选即翻译

状态：核心自动翻译与追问已实现，真实订阅通道及侧栏已验证；详细结果与未验证边界见 [V0.3 验证记录](docs/V0.3_VALIDATION.md)。下文保留迭代设计依据与原验收目标。
日期：2026-09-10。
依据：用户实际体验反馈“截完图还要复制两遍，太麻烦；希望截完图就给出翻译”。

## 1. 版本目标与验收标准

配置完成后，正常阅读只需要：

按截图翻译快捷键 → 框选并松开 → 侧栏自动出现译文。

截图完成后不再要求点击“准备给 AI”、复制图片、复制说明、切换聊天窗口或手动发送。
保留等待模型响应的真实状态；“自动开始”不等于“松手瞬间全文完成”。

本机目前截图翻译为 Ctrl+Alt+Q，解释为 Ctrl+Alt+A，文字为 Alt+Shift+Q。保留默认 Alt+Q／Alt+A 与现有冲突回退机制，界面始终展示实际快捷键。

V0.3 的核心是获取并显示真实 AI 结果。连续追问为第二优先级，知识图谱和 OCR 不扩展。

## 2. 先修正上一轮的接入判断

V0.2 评估了聊天网页手动交接及 MCP，遗漏了官方 Codex App Server。因此“两次复制”可以保留为故障备用，但不应继续作为下一版正常阅读流程。

已核实的官方能力：App Server 面向自有产品集成，支持本地图片输入、回复事件及官方托管的 ChatGPT 登录；认证文档区分订阅登录与 API Key 按量付费。参考 [App Server 官方文档](https://learn.chatgpt.com/docs/app-server)、[官方认证文档](https://learn.chatgpt.com/docs/auth)。

本机只读核验：codex-cli 0.153.4，存在 app-server 与 stdio 入口。设计阶段尚未调用模型。实现阶段已通过官方 ChatGPT 登录验证 gpt-5.6-luna 图片翻译和回答回传；使用自制测试图，没有读取凭据。具体延迟和限额边界见验证记录。

注意：本机命令帮助及官方文档标记了实验性能力。它是本项目个人使用版本的首选验证路线，不直接视作已经稳定可交付的通用产品接口。需记录兼容版本并做接口变更检测。

## 3. 接入方案选择

| 路线 | 能否满足目标 | V0.3 决策 |
| --- | --- | --- |
| 官方 Codex App Server + ChatGPT 订阅登录 | 文档具备图片输入与结果回传所需能力；本项目端到端待验证 | 首选，先完成小规模可行性验证 |
| 用户自选视觉 API | 可作为独立模型适配方向；需配置与额外费用评估 | 可选备用，不强制购买、不自动切换到付费 |
| 本地视觉模型 | 需匹配显存、模型、速度及 STEM 准确率 | 后续选择，本轮不先下载大型模型 |
| 浏览器复制、粘贴和点击自动化 | 会依赖焦点、页面结构与账号客户端行为 | 不作为正常路径 |
| 原两步剪贴板 | 可在服务不可用时使用 | 收入“更多／手动交接”，不阻挡正常翻译 |

首选路线使用订阅可用的 Codex 能力，按对应账户额度和权限运行；不等于无限免费，也不承诺与 ChatGPT 网页中的模型、对话历史、记忆完全一致。不能把网页登录态拿来当通用 API Key。

## 4. 用户流程

### 首次连接，仅做一次

设置中增加“自动翻译服务”：

- 检测官方 Codex 命令与版本。
- 使用官方提供的认证状态接口检查连接方式，不读取或解析 auth.json。
- 必要时打开官方登录流程，由用户完成；凭据由官方客户端管理。
- 若检测到 API Key 方式，明确提示这不是订阅方式，不静默按费用路径发送。
- 明确告知：“开启后，主动框选的图片及当前项目必要上下文会发送到所选服务，用于翻译。”
- 用户启用“框选后自动翻译”，此后每张图不再重复弹出授权框。
- 保留“仅本地截图”模式；连接失败时保留当前图片并提供重连入口。

这是 V0.2 “不自动上传”的明确模式扩展。已启用自动翻译后，主动截图就是本次所选区域的发送操作；后台不抓屏、不开连续监视。

### 日常翻译

1. 用户阅读 PDF，按快捷键框选。
2. 侧栏恢复，先显示小图与“正在翻译……”；不覆盖系统剪贴板。
3. 当前图片、翻译任务和已有最小上下文自动提交。
4. 收到可显示的回复后逐步呈现译文，完成后显示操作栏。
5. 用户继续阅读或再次框选，无需整理 AI 输入。

无项目、无资料时照常翻译，不弹出必填表单。可在之后补充归属；已发出的请求保持发送时的项目快照。

### 解释与追问

- 截图解释快捷键：直接发送解释任务。
- 已有截图点击“解释这段”：沿用图片，直接解释，不重截。
- 输入追问后 Enter 发送；Shift+Enter 换行。
- 发送当前截图、必要译文和最近少量问答，不无限累积历史。
- 新截图开启新的阅读卡片；只有用户明确要求“结合上一张”才带上一张图片。
- 切换项目清空活动请求与显示绑定，防止跨项目混入历史结果。

## 5. 侧栏设计

当前截图中的大缩略图和交接按钮占据主要空间。V0.3 把大部分空间用于实际译文。

~~~text
StudyCopilot
模拟电子技术 · Microelectronic Circuits        设置

[ 小截图预览，可点击放大 ]       中文翻译
──────────────────────────────────
1 模拟设计导论
1.1 为什么选择模拟设计？
1.2 为什么选择集成电路？
1.3 为什么选择 CMOS？
……
──────────────────────────────────
[解释这段]   [复制译文]   [记住这个]

继续提问……                           发送

[重新截图]                     更多
已连接 · 自动翻译
~~~

上面是使用用户图片目录结构表达的静态界面示意，不是已经调用模型得到的结果。

布局要求：

- 缩略图默认高度约 96–140 个逻辑像素；点击放大或展开原图。
- 译文区域占主体空间，至少明显大于预览区域；图多时可展开原图阅读。
- 翻译开始即显示结果区域，避免用户一直面对功能按钮。
- 用户滚动回看时不强制抢回底部；只有停留在底部才跟随新增文字。
- 普通界面不显示 Prompt、JSON、工具事件、计划或推理过程。
- 项目设置、知识、记忆和完整说明继续折叠。
- “准备给 AI／复制图片／复制说明／打开 AI”移入“更多 → 手动交接”。
- 单击“复制译文”复制实际回答，不能复制内部上下文冒充结果。
- 第一阶段保证段落、列表、表格和变量可读；不能把 LaTeX 原始转义串宣称为排版完成。复杂公式渲染若增加依赖，单列后续改进并保留原图对照。

## 6. 异步任务与错误状态

每次请求绑定 request_id、screenshot_id、project_id、source_id、session_id、task_type 和输入快照。

状态：
ready → sending → generating → completed
并允许 cancelled、failed、needs_login、rate_limited。

规则：

- UI 不阻塞，网络／子进程等待有超时，可以停止。
- 截图取消不发请求、不消耗模型调用。
- 重复事件只触发一次请求；同一图片的重试由用户明确点击。
- 新截图取消旧活动请求；即使旧事件晚到，也不能覆盖新截图的结果。
- 项目切换、清除截图、窗口关闭后，旧结果不再更新当前页面。
- 不自动无限重试或偷偷换模型；限额不足显示原因和可恢复时间（仅当服务实际返回）。
- 流式途中失败可保留已收到部分文字，并标明“未完成”，不能当成成功记录。
- 服务断线时保留截图，可重连后重试；手动交接是备用入口。
- 翻译与解释采用不同模板，默认翻译不自动总结或讲解整章。
- 如果模型要求运行工具才能继续，终止该翻译请求并报告内部兼容问题，不能让翻译流程变成执行命令。

性能目标为工程目标，必须实测，不作服务 SLA：

- 截图完成后 300 ms 内显示可见等待状态；
- 典型一段正文尽量在 3–8 秒内出现首批可读译文；
- 用 10–20 张固定样例记录冷启动／热启动、首字／全文耗时和成功率；
- 大图、复杂公式、网络波动、额度限制单独报告；不写“秒出”掩盖实际等待。

## 7. 最小技术改动

复用 RegionCapture、ScreenshotStore、StudyContext、ContextPackage、ContextManager、MemoryRetriever 和现有模板，不重建项目。

新增协调层 ReadingTaskController：
- 接收截图成功事件；
- 在已启用自动翻译且连接可用时提交；
- 管理请求 ID、取消、结果绑定和有限的追问上下文。

扩展 providers 边界：
- 保留兼容的基础接口；
- 新增事件式生成能力，向 UI 暴露开始、文字片段、完成、失败；
- Provider 接收厂商无关请求快照，不让 UI 依赖 Codex 协议；
- 提供 FakeProvider 验证交互，但假结果必须显著标记测试，不能作为真实能力验收。

新增 CodexProvider：
- 通过 Python／Qt 子进程管道对接官方 App Server，不开对公网监听端口；
- 使用官方托管登录；不复制 Token、不抓 Cookie、不接管当前 Codex 桌面任务；
- 使用本应用专属请求与工作目录，不连接开发项目的活动线程；
- 以安装版本导出的协议核对字段，再编写适配；不把网页示例当成已验证的本机协议；
- 从可用能力中选支持图片的模型，不在设计阶段写死一个未经实测的型号；
- 分离面向用户的最终回答与代理状态文本；
- 检查客户端自身的历史和日志行为，不能只因 StudyCopilot 日志不存正文就声称所有层都不保存；
- 用可执行权限控制限制工具、读取范围和外部连接，不能只靠 Prompt 说“不要执行命令”。隔离无法可靠实现时，不把这条路线标为可交付。

这里的权限与缓存隔离属于实现前必须验证的技术条件；本轮只设计，不改动用户现有 Codex 配置或账号。

## 8. 本地结果与现有数据

首次连通性验证不改 schema，结果可暂存内存。

完成验证后再考虑增量 migration：
- 新增 reading_requests／reading_results 或在审查原 translation_history 后最小扩展；
- 保存请求身份、截图关联、任务类型、实际服务／模型、状态、时间和明确完成的回答；
- 流式片段不逐字写库，完成后一次提交；失败不得标记成功；
- 最近结果是短期阅读记录，与长期 Memory 分开；
- “记住这个”只保存用户明确选择的内容；
- 短期记录与临时截图共同采用有限保留策略，避免每次自动翻译都变成永久图片；
- 保留的学习卡片才提升图片持久状态。

完整保留 v1／v2 用户数据，迁移前备份，失败回滚。不要绕过 ScreenshotStore 直接写图或删图。

## 9. 实施顺序

### V0.3a：先证明能自动得到译文

先用合成图，完成“官方订阅登录 → 图片输入 → 返回真正译文”的最小验证。
检查 Windows 版本兼容、权限隔离、模型图像支持、额度、首字延迟以及官方组件缓存。

通过条件：无需复制粘贴、无需浏览器点击发送、无需另购 API，即可在本应用接收真实回答。
如果失败，记录具体阻塞及官方错误；不把未连通的 UI 称为完成，不悄悄切换到收费服务。

### V0.3b：接入截图与结果侧栏

复用现有截图成功事件，自动提交，增加结果区域、停止／重试和旧请求丢弃。
完成默认界面重排，手动交接移入更多操作。通过后才升级正式使用流程。

### V0.3c：同图追问与学习保留

围绕当前截图添加有限追问、解释按钮、复制译文和选择性保存。
先做好本图连续理解，不扩展多模型并发、全书 OCR、跨项目知识图谱或复杂聊天管理。

## 10. 验收测试

保留现有 93 项回归测试，并增加：

1. 配置完成后，截图成功仅触发一次自动请求。
2. 无项目、无资料、只有图片均可翻译。
3. Esc 取消截图不上传；停止生成可中止当前任务。
4. 首个可显示片段在完成前出现，部分结果不冒充完成。
5. 图片 A 请求晚到不能覆盖图片 B。
6. 项目切换后旧项目结果不得写进新项目页面或上下文。
7. 默认翻译不自动总结；解释按钮和追问使用正确任务。
8. 未登录、断网、超时、限额、损坏图片、协议不兼容有清楚状态和重试路径。
9. 未启用自动翻译时仍只本地截图；默认流程不改剪贴板。
10. Prompt、图片中的指令不能触发读文件、执行命令或打开其他连接。
11. 原始图片、变量、上下标与公式在输入端没有被 OCR 替换或不可读缩小。
12. 日志、短期记录、长期 Memory 与官方客户端历史的边界和保留策略核对。
13. 实际 PDF 10–20 张样例涵盖正文、公式、电路图、图注；测得延迟和准确性。
14. 确认真正完成“框选 → 本应用译文”，不能以 MockProvider 输出代替真实端到端验收。

## 11. 原设计阶段交付边界（历史）

此节记录实现前的设计状态；当前 0.3.1 已完成自动翻译、同图追问与延迟优化，见 [当前状态](docs/STATUS.md)。

设计阶段已完成：体验目标、主界面、官方接入路线核实、本机命令能力核验、实施顺序及验收条件。
设计阶段尚未完成：账号连接验证、模型调用、图片上传、自动译文实现。
现有 V0.2 程序和数据库保持运行逻辑不变。

下一步开发从 V0.3a 开始。不能再把压缩手动复制步骤当作“框选即翻译”的完成标准。
