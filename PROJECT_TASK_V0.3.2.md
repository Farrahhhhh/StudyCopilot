# StudyCopilot V0.3.2 — 可见、可控、可验证的项目学习记忆

日期：2026-09-14。用户明确授权的 V0.3 增量阶段；不扩展模型服务或采集范围。

## 核查结论与范围

开发前以代码和 137 项基线测试核查，而非以需求描述判断完成情况：

| 分类 | 实际状态 |
| --- | --- |
| 已实现且有测试 | 主动截图／选文 → Project、Source、Session → ContextManager → ReadingWorkflow → CodexProvider → 侧栏流式回答 → 完成时保存 reading_results；取消与过期事件隔离；20 张临时图与 60 条完成回答上限；迁移备份与外键 |
| 已实现且接入真实请求 | FTSMemoryRetriever 和概念别名匹配，ContextPackage.relevant_memories 已由 format_reading 写入请求；用户手动保存 memories 并关联保留截图 |
| 只有数据／接口，未作为当前阅读结果流程使用 | questions、translation_history 旧预留表；Concept Links 没有自动图遍历。不得宣称这些表形成了长期记忆闭环 |
| 原来未实现 | 疑问状态、编辑删除管理、发送时使用记录、记忆开关、截图手动选记忆、阅读结果来源引用及历史清理后的回溯 |
| 原来存在的问题 | 通用 FTS 允许 global 记忆与较弱词匹配；详情显示随数据库变化的候选，不能证明回答实际使用了哪些记忆 |

现有真实服务验证记录仍见 docs/V0.3_VALIDATION.md；这是历史验证证据。本阶段只执行模拟服务测试，不将历史真实测试等同于本阶段已通过真实端到端验收。

实施顺序：复用 memories 与迁移 → 严格项目检索 → 实际发送快照 → 主动保存／管理／来源 UI → 回归与离线界面检查 → 中文说明。

## 本阶段验收约束

1. 当前项目、资料始终可见；当前回答使用的记忆来自请求快照，不能来自重建后的候选列表。
2. 用户主动编辑并保存疑问，允许可选概念和别名、待解决／已解决、已知页码章节；不调用模型总结，不推断用户掌握程度。
3. 当前项目内可靠匹配、待解决优先，最多 5 条、序列化总长最多 6000 字符。无匹配就不带入。
4. 截图没有文字线索时允许填写概念或手动指定待解决记忆，仍只进行原本的阅读请求。
5. 关闭开关后新请求不携带长期记忆；当前问题和正常追问上下文保留。来源记忆已停用／修改的旧回答不通过追问历史重新携带。
6. 完成结果与所用记忆快照一起保存；旧结果无快照时明确标为未知，不根据今天的数据库补造历史。
7. 来源清理不得级联删除记忆；缺失来源明确提示，正文仍可检索。删除记忆不删除阅读结果或已保留图片。
8. 数据库升级前备份、事务迁移、外键开启；不删除用户数据、不覆盖旧测试。
9. 保留原有快捷键、剪贴板恢复、主动截图、自动发送 opt-in、临时无工具请求、官方托管认证、严格请求归属和日志隐私边界。
10. 不新增 OCR、额外识别调用、付费 API、自动总结、后台监控、阅读器、云同步或知识图谱。

## 验证和实现说明

实现取舍、测试结果和可复现手动步骤见 [项目记忆实现与验证](docs/V0.3.2_MEMORY.md)。原有 V0.1/V0.2/V0.3 要求继续保留；此阶段授权优先于其中旧的记忆界面限制。
